# Example environment file for VideoJS Player project

# Storage configuration
STORAGE_TYPE=local
STORAGE_DIR=./storage
NFS_MOUNT_POINT=/mnt/nfs

# Database configuration (for non-Docker setup)
DB_HOST=10.1.1.10
DB_PORT=5432
DB_NAME=video_db
DB_USER=video_user
DB_PASSWORD=Astr0mao@db

# JWT secret key
JWT_SECRET=your_jwt_secret_key

# Backend API port
BACKEND_PORT=3001

# Frontend development port
FRONTEND_PORT=3000
